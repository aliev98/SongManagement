@model IEnumerable<SongsDomain.SongForPlaylist>

@{
    ViewData["Title"] = "Index";
}

<h1>Songs in playlists</h1>



<p>
    <a asp-action = "Create">Add a song to a playlist</a>
</p>

<table class="table">
    <thead style="background-color:aliceblue">
        @*<tr>
                <th>
                    @Html.DisplayNameFor(model => model.SongDetails)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.Playlist)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.Playlist)
                </th>

                <th></th>
            </tr>*@

        <tr>
            <th>Song name (Id)</th>
            <th></th>
            <th>Playlist name (Id)</th>
            <th></th>
        </tr>

    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SongDetails.Title) @Html.Encode($"({item.SongDetailsId})")
                </td>

                <td>
                    <p>In the playlist</p>
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.Playlist.Name) @Html.Encode($"({item.PlaylistId})")
                </td>

                <td>
                    @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |*@

                    <a asp-action="Delete" asp-route-id="@item.Id">Remove</a>
                </td>
            </tr>
        }
    </tbody>
</table>
